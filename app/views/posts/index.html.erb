<div id='posts-index'>
  <% if @posts.present? %>
    <%= render partial: 'shared/post', collection: @posts, as: :post %>
  <% else %>
    <div>現在募集はありません</div>
  <% end %>
</div>
<%= paginate @posts %>

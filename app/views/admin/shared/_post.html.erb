<div id="admin-post-index-<%= post.id %>">
  <span><%= post.id %></span><span><%= post.quest_name %></span><span><%= post.user.name %></span>
  <%= link_to '削除する', admin_post_path(post), remote: true, method: :delete, class: 'btn btn-danger' %>
</div>